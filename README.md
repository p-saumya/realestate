required installation : python(django) , react , pip , npm 
installation steps :
  cd frontend
  npm install react
  
Steps to run the program:
  Terminal 1:
    cd backend
    pip install -r requirements.txt
    python manage.py runserver

  Terminal 2:
    cd frontend
    npm start
